# 强制 shell 脚本使用 LF 换行，避免在 Windows 上被 git 转成 CRLF
# 导致 Linux 服务器执行时出现 \r 错误
*.sh text eol=lf
deploy.sh text eol=lf

# Windows 批处理使用 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.db binary
*.sqlite binary
*.sqlite3 binary
